# V1.1 Workbench Router Namespace

This directory is the router namespace for `/api/v1/*`.

Stage 0 scope:

- namespace confirmation
- health route baseline in T0-02

Workbench routers must not depend on Legacy API modules.


# V1.1 Workbench Service Namespace

This directory is the service namespace for V1.1 Workbench.

Allowed service names:

- `WorkService`
- `ChapterService`
- `SessionService`
- `IOService`
- `WritingAssetService`

Workbench services must not call Legacy services.


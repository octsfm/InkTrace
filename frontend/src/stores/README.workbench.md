# Workbench Store Naming

V1.1 Workbench store filenames reserved by the Stage 0 baseline:

- `workspace.js`
- `chapterData.js`
- `saveState.js`
- `writingAsset.js`
- `preference.js`

Store shells are initialized in T0-08. Workbench stores must not import Legacy
stores.

